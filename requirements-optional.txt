# Optional / heavier dependencies.
# Install with: pip install -r requirements-optional.txt
torch>=2.2            # LSTM forecaster
pmdarima>=2.0.4       # auto_arima (statsmodels fallback exists if absent)
pytorch-forecasting>=1.0   # Temporal Fusion Transformer (advanced, optional)
lightning>=2.2
pytest>=8.0
pytest-cov>=5.0
ruff>=0.4
jupyter>=1.0
nbformat>=5.9
